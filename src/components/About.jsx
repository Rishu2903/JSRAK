import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";
import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        At JSRAK Software Solution, we are a dedicated and results-driven team passionate about delivering innovative and scalable software solutions. Specializing in TypeScript, JavaScript, and modern frameworks like React, Node.js, and Express.js, we focus on building high-quality, production-grade applications tailored to meet diverse business needs.
        Founded with a commitment to excellence and innovation, JSRAK Software Solution leverages industry best practices and cutting-edge technologies to deliver solutions that drive tangible results. Our expertise spans full-stack development, ensuring end-to-end service that combines technical proficiency with a user-centric approach.
        <br className='sm:block hidden' />
        As a forward-thinking company, we prioritize continuous learning and staying updated with the latest industry trends to bring the best solutions to our clients. Whether it's a startup looking to establish its digital presence or an enterprise seeking to enhance operations, JSRAK Software Solution is your trusted partner for success.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
